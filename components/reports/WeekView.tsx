import { staffReport } from "../../utils/mockData";

interface WeekViewProps {
  data: staffReport[];
}

const WeekView = ({ data }: WeekViewProps) => {
  return <>{/* Data will be processed here and mapped to report Item  */}</>;
};

export default WeekView;
